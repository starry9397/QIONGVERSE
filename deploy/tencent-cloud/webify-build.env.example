# CloudBase Webify public build variables only.
# Replace the placeholder with the final HTTPS Webify/custom domain.
VITE_PUBLIC_SITE_URL=https://<actual-webify-domain>
VITE_LUOYIN_API_BASE_URL=https://qiongverse-api.onrender.com

# Never place GLM_API_KEY, OAuth secrets, or server-only variables here.
